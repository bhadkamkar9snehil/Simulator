Bundled Python runtime for the simulator suite.

- Launchers must use `runtime/python/python.exe` or `runtime/python/pythonw.exe`.
- Do not replace this runtime without also rebuilding `wheels/wheelhouse_manifest.json` and `wheels/wheelhouse_constraints.txt`.
